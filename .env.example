# Copy to .env (git-ignored) and fill in. bin/geo and every make target
# source this file automatically. chmod 600 .env after filling it in.

# Telegram alerts (required for any run you care about)
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# Anthropic API key -- OPTIONAL with the default classifier.provider
# claude_cli (which uses your Claude subscription via the `claude` CLI:
# install it and run `claude login`). If set, it serves as an automatic
# fallback when the CLI fails; required only for provider: anthropic.
ANTHROPIC_API_KEY=

# Polymarket CLOB credentials (required only for --live execution;
# paper/alert_only runs never need these)
# POLYMARKET_PRIVATE_KEY=
# POLYMARKET_FUNDER_ADDRESS=
